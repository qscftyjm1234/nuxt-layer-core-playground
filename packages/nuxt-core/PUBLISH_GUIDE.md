# NPM 套件發布指南 (softleader-nuxt-core)

本文件說明如何正確、安全地將 `softleader-nuxt-core` 發布到 NPM。

## 準備工作

1.  **確保登入**:
    在終端機執行 `npm whoami`。如果沒有登入，請使用 `npm login`。
2.  **Lint & Type Check**:
    確保程式碼通過檢查，避免發布錯誤版本。
    ```bash
    npm run lint
    npm run typecheck
    ```

## 發布步驟

### 步驟 1: 更新版本號

使用 `npm version` 指令來更新版本，這會自動修改 `package.json` 中的 `version` 欄位。

- **Patch (補丁/小修正, 1.0.0 -> 1.0.1)**:
  ```bash
  npm version patch
  ```
- **Minor (新增功能, 1.0.0 -> 1.1.0)**:
  ```bash
  npm version minor
  ```
- **Major (破壞性變更, 1.0.0 -> 2.0.0)**:
  ```bash
  npm version major
  ```

### 步驟 2: 預覽發布內容 (選填)

為了確保發布的檔案正確，建議先執行以下指令：

```bash
# 預覽會被打包的檔案
npm pack --dry-run
```

這會列出 `package.json` 中 `files` 欄位指定的檔案。

### 步驟 3: 正式發布

```bash
npm publish
```

> [!TIP]
> 如果這是私有 Scope 套件 (例如 `@org/package`) 且是第一次發布，可能需要加上 `--access public`：
> `npm publish --access public`

## 🚨 安全重要事項 (核心規範)

> [!CAUTION]
> **絕對不要將包含 authToken 的 `.npmrc` 檔案推送到 Git 遠端倉庫。**

- 本專案已將 `.npmrc` 加入 `.gitignore`。
- 如果你需要本地設定，請手動建立檔案，但切記不可使用 `git add .npmrc`。
- 如果不慎推送到 GitHub，請立即撤銷該 Token 並通知管理員。

## 常用連結

- [NPM 官方網站](https://www.npmjs.com/)
- [NPM 官方文件](https://docs.npmjs.com/cli/v10/commands/npm-publish)
- [軟體版本概念 (SemVer)](https://semver.org/lang/zh-TW/)
