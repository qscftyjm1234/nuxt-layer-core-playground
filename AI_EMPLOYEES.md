# AI 員工說明書 (AI Employees Guide)

在本專案中，我們擁有多位具備不同專業的工作流（Workflows），您可以根據任務需求「指派」給對應的 AI 員工執行。

## 您的 AI 員工清單

### 1. 核心架構師 (/architect-sync)

- **職責**：處理核心架構同步。當您修改了 `packages/nuxt-core` 的基礎邏輯或配置，並希望同步到所有子專案時，請呼叫他。
- **何時使用**：核心配置變動、通用組件更新。

### 2. 錯誤排查員 (/error_checker)

- **職責**：自動化診斷與修復。當您遇到終端機報錯、TypeScript 錯誤或建置失敗時，他是最佳助手。
- **何時使用**：出現 `Exit code: 1`、`Module not found` 或任何紅字時。

### 3. 套件管家 (/safe-package-update)

- **職責**：安全更新 npm 套件。他會處理版更新、備份、並在失敗時自動還原，確保開發環境穩定。
- **何時使用**：需要新增、更新或降級依賴項時。

### 4. 文件同步員 (/sync-ui-docs)

- **職責**：確保技術文件與 UI 展示一致。當核心 README 或技術文檔變動時，他會自動同步到 `ui-docs` 專案。
- **何時使用**：修改了架構文檔或新增了核心組件說明時。

## 使用建議：我該請誰做？

如果您有 10 個 AI 員工，您可以這樣指派：

1. **「核心改了，幫我同步到所有地方」** ➔ 指派給 **核心架構師** (`/architect-sync`)
2. **「這裡噴錯了，我看不懂」** ➔ 指派給 **錯誤排查員** (`/error_checker`)
3. **「我想更新這個套件，但怕壞掉」** ➔ 指派給 **套件管家** (`/safe-package-update`)
4. **「改了功能，別忘了更新文件」** ➔ 指派給 **文件同步員** (`/sync-ui-docs`)

## 指令使用方法

在聊天框輸入斜線指令即可啟動（例如：`/architect-sync`）。

> [!IMPORTANT]
> **架構原則**：所有員工都遵循「以 `packages/nuxt-core` 為權威來源」的原則進行操作。
